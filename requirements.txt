# Core
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
plotly>=5.17.0

# Resume parsing — PDF
PyPDF2>=3.0.0

# Resume parsing — DOCX (Word documents)
python-docx>=1.1.0

# Resume parsing — Images & scanned PDFs (OCR)
Pillow>=10.0.0
pytesseract>=0.3.10
# pdf2image>=1.16.3   ← uncomment if you want scanned PDF support
#                       Also needs: apt install poppler-utils  (Linux)
#                                   brew install poppler       (macOS)
#                                   Download poppler for Windows
